# Erste Nachricht für den neuen Claude-Code-Chat

Vorher auf dem PC:
1. claude-mem ausschalten: `claude` starten → `/plugin` → claude-mem → **Disable** → `/exit`
2. Den Ordner `ai-creator` aus GitHub nach `C:\ai-creator` kopieren (siehe Antwort im Cloud-Chat)
3. In `C:\ai-creator` mit `claude` starten und das hier einfügen:

---

Lies CLAUDE.md, MODELLWAHL.md, ANLEITUNG.md und STATUS.md vollständig. Wir starten neu. Der alte Chat ist ausgewertet, seine Fehler stehen in CLAUDE.md unter „Lehren aus dem alten Chat“, und die Lösungen dort sind verbindlich.

Die Entscheidungen in CLAUDE.md stehen fest. Schlag keine anderen Modelle, keinen Face-Swap, kein PuLID und keine zwei LoRAs vor. Workflow, Trainings-YAML und Skripte sind bereits geprüft. Pass sie nur gezielt an und zeig mir jede Änderung.

Mach als Erstes genau das:
1. Fass in maximal 10 Zeilen zusammen, wie du die Pipeline verstanden hast.
2. Sag mir, welche Dateien aus `C:\iCloudDrive\ai-modell-v2\` du für Phase 1 brauchst (Gesichtsdatensatz v2, die 95 Körperbilder, `gesichter30.py`, `setup_neu.sh`), und prüf, ob sie da sind.
3. Starte mit dem aktuellen Schritt aus STATUS.md. Sag vorher, was passiert, wie lange es dauert und was es kostet.

Danach stoppen und auf mein OK warten.
